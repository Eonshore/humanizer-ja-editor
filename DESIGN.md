# Design decisions

## v0.6.0で追加したなんJスレッド風

- `nanj-thread` を、明示指定時だけ口調とレス形式を決めるプロファイルとして追加する。処理モード、場面、読者、目的の各層は維持する
- 語尾の一括変換ではなく、アンカーを使う返答、短レスと説明レス、話者の関心の違いでスレッドの流れを作る
- 架空の話者・質問・相づち・番号は、依頼された形式の演出として扱う。対象の事実、実在の人物の体験や感情、世評、出典は追加しない
- 元資料の主体、確度、条件、数字、引用、コードはスレタイと各レスでも保持する。レス番号と本文の数量は分けて監査する
- 「口調だけ」「行数維持」「監査だけ」を優先し、GitHubなどのスレッドという語だけでは適用しない
- 定型ネタやレス数を固定せず、ノリの強さは指定と材料に合わせる。毎回同じボケ・ツッコミ・総括へ揃えない

## v0.1.0から維持する既定値

- 文章の「AIらしさ」より、意味・責任・確度・数字の保存を優先する
- 日本語を主対象とする
- Agent Skillsのportable subsetへ合わせる
- `SKILL.md`は中核手順に限定し、詳細は`references/`へ分割する
- 既定モードは`rewrite`
- 既定出力は完成版一つ。診断や変更理由は要求された場合に出す
- 場面を7種類へ分け、技術・学術・小説では保守的に編集する
- 作者サンプルがある場合、一般規則より優先する
- 元文にない体験、感情、数字、出典、固有名詞を追加しない
- 単語の禁止ではなく、密度・反復・用途不一致で判定する
- AI生成判定や検出回避を目的にしない
- 補助スクリプトはPython標準ライブラリだけで動かす

## v0.2.0で追加した読者フロー

- 読みやすさを、文長だけでなく文同士の関係を予測できるかで見る
- 対比、例外、因果などを読み違える場所だけ、関係を先に示す
- 読者に生じた疑問は、原文にある情報の順序で回収する
- 指示語は全面削除せず、受け先が一つに定まる場所で文をつなぐ
- 問題から改善へ移る境界は示すが、原文にない負担、利点、因果は足さない
- 即席の複合語や万能動詞は、原文から具体的な動作を特定できる場合だけ直す
- 定義済みの「正本」など、検索や運用に役立つ用語は語形だけで削除しない

## v0.3.0で追加した概念先行のソフトウェア解説

- リポジトリ、コード、テスト、既存文書、ベンチマーク出力、ログから技術説明を作る `document` モードを追加する
- `document` モードは、資料化や技術説明を明示的に求められた場合だけ選ぶ。文書化を求めないコードやログだけの入力には、従来どおり自動適用しない
- 根拠・事実資料、作者の声のサンプル、説明構造のサンプルを分ける。声や構造のサンプルは、別対象のドメイン事実、実装、性能、到達状態を供給しない
- `profile_author.py` の定量的な声の指標と、読者、目的、中心問い、概念的骨格、抽象化の境界、証拠の見せ方を決める説明プロファイルを分離する
- 設計・文書上の主張、ソースの存在、到達可能な経路、テストまたは再現、ベンチマークまたは性能、実行中または利用者に見える運用を別の根拠段階として扱う
- 本文は問題と整理の軸から始め、仕組み、条件、証拠、限界を読者の問いに沿って並べる。ディレクトリ、モジュール、関数の順に説明しない
- 公開契約、必要な証拠アンカー、省くと曖昧になる識別子だけを本文に残す。内部ヘルパーや局所的な実装詳細は、必要なら注記、付録、証拠表へ移す
- APIリファレンス、モジュール参照、呼び出し経路を明示的に求める場合は、公開面と構造を主にしてよい。ただし、この例外でもソースの存在を実行済みの動作や性能へ言い換えない
- 目的、読者、根拠が不足する場合は、安全な狭い仮定をラベル付けする。仮定で結果が変わる場合は、完成文を捏造せずアウトラインと証拠の不足箇所を返す
- 概念的骨格は、問題または問い、動作原理、仕組み、条件、証拠または結果、限界または非目標、次の検証または範囲を限定した定義を既定候補とする。完成物と資料に応じて変えるため、固定テンプレートにはしない

## v0.4.0で追加した初心者向け技術説明プロファイル

- 初心者向け説明を新しい処理モードや場面にせず、`document`、`rewrite`、`edit` へ重ねる任意の説明プロファイルとして扱う
- 「初心者」を対象分野の語彙や慣習をまだ共有していない読者と定義し、理解力や判断力まで不足しているとは扱わない
- 通常の説明プロファイルに、初心者の範囲、既知の足場、読後目標、最小ケース、正確さへ戻る地点、図解の可用性、詳細の予算を追加する
- 複雑なアーキテクチャ、アルゴリズム、システム観察、トレードオフ説明の四つの構造候補を用意するが、節数や問いかけを固定しない
- 暫定的な単純化、心的モデル、比喩は、正式な用語、条件、仕組みへ戻す。分かりやすさを理由に根拠段階、例外、確度を弱めない
- 実際に提示しない図、アニメーション、操作UIを参照しない。図を出せない場合は、短い文字列フロー、表、番号付き手順へ置き換える
- 比喩は目的、対応関係、限界を揃え、事実、定義、因果関係の代わりにしない
- 公開記事からは一般的な説明構造だけを抽出し、対象の技術事実、図、数値、著者固有の口癖、一人称の体験、企業や製品の販促表現を持ち込まない
- 専門語、API、CLI、設定キーは、必要性を先に示したうえで原表記を残す。初心者向けを理由に検索性や利用契約を壊さない

## v0.4.1で追加した一般向けの入口

- 利用者が最初に選ぶものを、対象、目的、保護条件の三つへ絞る
- READMEの冒頭では、文章の編集、問題点だけの監査、資料からの技術説明という三つの開始経路を先に示す
- `rewrite`、`annotation`、`edit`、`document`、場面、編集範囲、初心者向け説明方針は内部ルーティングとして維持し、初回利用の前提知識にしない
- `agents/openai.yaml` はUI上の表示名、短い説明、既定プロンプトだけを持ち、ツール依存や明示呼び出し専用のポリシーを追加しない
- 公開面の簡略化によって、保真契約、根拠段階、作者サンプルの優先順位、適用しない場面を弱めない

## v0.5.0で追加した目的プロファイル

- ガイド付きチュートリアル、トラブルシューティング、比較・選定を、新しいモード、場面、人格ではなく、`document`、`rewrite`、`edit`、`annotation`へ必要に応じて重ねる目的プロファイルとして扱う
- 場面は語域、初心者向けは読者の前提、目的プロファイルは実行・診断・判断の情報構造、作者サンプルは声を決める。主目的に合う目的プロファイルを既定で一つ選び、複合した完成物が明示された場合だけ組み合わせる
- ガイド付きチュートリアルでは、到達点、開始状態、主要経路、操作、観察、失敗分岐、停止点、条件つき完了、後始末を分ける。手順、出力例、実行記録を区別し、文書化を実行権限または成功証拠へ変えない
- トラブルシューティングでは、依頼権限、期待と実際、観察、仮説、識別する検査、原因確度、回避策、最小修正、実環境での再検証、rollbackを分ける。設定済み、到達可能、実行中、利用者に見える状態を別の証拠段階として扱う
- 比較・選定では、判断課題、必須条件、共通軸、比較可能性、未知、優先順位の出所、ライフサイクル、推奨の強さ、証拠の確かさ、最終決定者を分ける。`unknown`を0点または劣位へ変換せず、根拠のない重みや総合順位を作らない
- 診断を修正権限へ、比較を購入・導入権限へ、チュートリアルを外部作用の実行権限へ読み替えない。文章の完成と、現実の操作、承認、受け入れを別にする
- 3プロファイルは、複数組織の公開資料から弱い一般構造だけを抽出する。対象固有の技術事実、数値、コード、評価基準、順位、原因、口癖、企業情報を持ち込まない
- `HJ-040`以降へ、実行済み捏造、観察不足、診断権限、相関と原因、回避策と根治、未知と劣位、重みの捏造、条件不一致、決定権、初心者向けとの合成を扱う回帰ケースを追加する
- パッケージ検証では3参照ファイルを必須にし、評価仕様に現れる目的プロファイル名を検証する

## 後から変更できる項目

### 発火範囲

現在は「AI臭を減らす」「自然な日本語へ」「文体に寄せる」など、明示的な編集依頼を中心に発火する。すべての文章生成へ常時適用したい場合は、frontmatterの`description`を広げる。

### public-essayの編集強度

既定は`bounded`。ブログやSNSでより強く構成を崩したい場合は`structural`へ変更できる。ただし、作者の立場を捏造しない制約は維持する。

### 作者プロファイルの保存形式

現状はJSON。将来、場面別プロファイル、複数作者、バージョニング、埋め込み検索を追加できる。

### 日本語解析器

現状の`profile_author.py`は形態素解析なし。SudachiPy、MeCab、GiNZAなどを任意依存として追加すれば、品詞比率、語彙分布、係り受けを精密化できる。

### 保真監査

現状は静的文字列監査。将来は次を追加できる。

- 固有表現抽出
- 数量と修飾対象の対応比較
- 主体・動作・対象の関係抽出
- entailmentによる命題保存評価
- diffとLLM judgeを組み合わせたCI

### 出力形式

現在は単一完成版。編集者向けに、JSON Patch、unified diff、問題箇所へのインライン注釈を追加できる。

## 初版で意図的に採用しなかったもの

- AIっぽい単語の一律禁止
- 文体を乱すための故意の誤字
- 必ず一人称や体験談を追加する規則
- 「人間らしさスコア」
- AI生成かどうかの判定
- 全角ダッシュ、太字、箇条書き、節番号の全面禁止
- 全文を二回生成する固定ワークフロー
- 読者の疑問を推測で補完する規則
- 接続詞や指示語を機械的に増減する規則
- 特定の単語をAIらしさだけで一律禁止する規則
- 問題と改善の差を見せるために、原文にない感情や利点を追加する規則
- コードの存在だけで、テスト済み、性能が出た、利用者に見える状態だと判断する規則
- 作者の声や説明構造のサンプルから、対象外の実装、性能、ドメイン事実を持ち込む規則
- すべてのソフトウェア解説を、ディレクトリや関数の順に説明する固定ワークフロー
- 初心者向け説明を、短文、疑問形、感嘆符、顔文字、称賛で演出する規則
- 比喩や暫定的な単純化を最終定義として残す規則
- 実在しない図、クリック、スライダー、アニメーションを参照する規則
- 特定の解説者、著者、企業媒体の口癖を合成人格として再現する規則
- チュートリアルに書かれた操作を、実行済みまたは成功済みへ変える規則
- すべての不具合説明を、単一の根本原因と長い事後報告へ揃える規則
- 設定保存、コマンド成功、デプロイ完了だけで、利用者向けの復旧まで証明したとする規則
- 比較資料で、未知を0点にし、根拠のない重み、星、総合順位を作る規則
- 比較、推奨、決定、購入、導入、公開を一つの権限として扱う規則

これらは誤修正、捏造、用途破壊、別のテンプレ文体への収束を起こしやすいため採用していない。
