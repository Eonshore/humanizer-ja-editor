# Humanizer JA Editor v0.8.0 検証結果

2026-09-05。v0.7.0の日本語編集に、なんJスレッド風の`nanj-thread`を統合した際の検証結果を記す。

## 統合した内容と静的検証

- なんJ用の参照資料と8件の評価仕様を取り込み、短いSKILLの入口へ条件付きの文体として接続した。
- やさしい日本語、会話の連続性、技術文書、原稿スタイルの4参照と、日本語統合の14件の評価仕様・8件の適用判断仕様を保持した。
- `benchmark.jsonl`は従来の51件となんJの8件を合わせた59件になった。既存ケースの内容は保持している。
- 単体テスト12件、package validator、Skill Creator quick validatorが成功した。

## 実際に生成した文章

編集過程を共有しない別のエージェント（gpt-5.6-terra）に4件を生成させ、実装側が実際の文章を確認した。

- **N01・標準の7レス**：本文では測定者、数量、条件、引用、コマンド、未確認状態を保持したが、題名に「らしい」を追加して確定した測定結果を弱めたため不合格とした。
- **N02・口調だけ**：3行と一人の文章を保ち、検証完了、公開予定、未承認、未実行の状態を維持した。レス番号を追加しなかった。
- **N03・問題点だけ**：検証範囲、検証者、公開時期の3件を指摘し、完成スレを生成しなかった。
- **N04・通常のGitHub返信**：一つの段落と敬体を保ち、バージョン、未確認状態、修正時期が未定であることを維持した。なんJの文体を適用しなかった。

N01の失敗を受け、なんJ参照に既にある確度の規則を、SKILLの完了確認にも題名と本文の両方を確認する条件として明記した。
別の資料でN01Rを生成し、7レス、測定者、同じサーバーという条件、360件、24秒と15秒、未検証の同時接続条件、10月3日の予定と未承認、逐語の引用、未実行のコマンドを確認した。題名も測定結果の確度を保っていた。
初回4出力と再確認1出力の計5出力を観察し、初回の失敗は成功として数えていない。

## 確認の限界

これらは代表例での確認であり、全73件の文章評価仕様をモデルで実行した結果ではない。「濃いめ」の生成、すべての読者条件との組み合わせ、複数モデルでの比較、Geminiによる実生成は今回の文章評価には含めていない。
パッケージ検証や導入ファイルの一致、スキルの発見、公開先で取得できることは、任意の文章で意味が保持される保証ではない。
最新コミットの自動検証は[GitHub ActionsのValidate](https://github.com/Eonshore/humanizer-ja-editor/actions/workflows/validate.yml)で確認できる。

## v0.7.0の日本語編集統合

2026-09-05。v0.5.0を基に、やさしい日本語、会話の連続性、技術文書の用語と論証、条件付き原稿スタイルを統合した際の検証結果を記す。

### 確認した範囲

既存の4モード、7場面、3目的プロファイルを保持した。分野の知識と日本語力は別の条件として選び、必要な参照資料だけを読む。
入口のSKILL.mdは315行から116行に整理した。開発環境では旧技術文書名の互換入口も確認したが、その入口はこのリポジトリに同梱していない。

- 既存の単体テスト11件が成功した。
- package validatorは警告0件で成功し、Skill Creatorのquick_validateも成功した。
- 統合時点では、開発環境の互換入口を含む相対Markdownリンク50箇所にリンク切れがないことを確認した。この件数は、その後の公開向け文書整理の前に数えたものである。
- 既存の評価仕様51件はバイト列を保持した。統合の評価仕様14件と、適用判断の仕様8件を追加した。
- 新規Codex app-serverのskills/listで、統合版Humanizerはuser scopeで1件、enabled=true、errors=[]だった。文章例の実行後も、新規プロセスで同じ状態を確認した。
- 文章例の評価中、CodexとGeminiの導入ファイルはマニフェストのハッシュを維持した。

### 実際に生成した文章

編集規則の作成過程を共有しない別のエージェントに6件を生成させ、評価側で意味と用途を確認した。

| 入力 | 初回の結果 | 確認内容 |
| --- | --- | --- |
| F-01 会話の継続 | pass | 共有済みの問題に応答し、毎回の称賛や総括を加えなかった |
| F-02 分野の初心者 | pass | 用語を説明し、テストファイルの存在を成功した証拠へ変えなかった |
| F-03 分野の専門家で日本語学習中 | fail | 並記された事実へ因果の接続を追加した |
| F-04 分野と日本語の両方を学習中 | pass | 両方の説明方針を使い、正式用語と実行予定を保持した |
| F-05 作者と既存形式の保持 | pass | 常体、同じ段落、指定表記、引用、未確認状態を保持した |
| F-06 旧技術文書名 | fail | 指定された原稿書式を適用しきれず、段落も分割した |

適用判断の8件では、文章作成・編集の4件と、逐語訳・誤字だけ・診断だけ・事実確認だけの4件が、事前に固定した期待と一致した。
これはモデルによる適用判断の例であり、Codexの自動選択率を測定した結果ではない。

### 観察された失敗への修正

並記や「であり」から因果を作らない条件を保真契約へ追加した。原稿スタイルには、明示された書式指定の効力、一文一行と段落の違い、一般名詞の並列と保護対象、文中コマンドと独立コード例の違いを明記した。

- T-03では別入力の並記事実に因果を追加せず、期限、主体、予定、未確認、未承認を保持した。
- T-05では作者が指定した表記、段落、引用、インラインコマンドを保持した。
- T-06では原稿書式の適用がなお不十分だったため、該当する書式の区別だけを補足した。
- 最後に別の独立した文脈でM-01/M-02を生成した。旧名を使うM-01では並列語を整えて一文一行にし、同じ段落を保った。保持指定があるM-02では元の表記と段落を保った。両方で引用内の中黒、インラインコマンド、未実行状態を保持した。

初回6出力、再確認3出力、最後の独立確認2出力の計11出力を観察した。途中の失敗を成功として数えず、修正した条件の再確認と区別している。

### 証拠の限界

これらは限定した文章例での確認であり、任意の文章で同じ品質になる証明ではない。65件の文章評価仕様すべてをモデル実行した結果でもない。
Geminiではファイル配置の一致を確認したが、Geminiによる実生成は確認していない。実環境の復元・再導入試験は実施していない。

この報告は統合時の文章品質と導入確認を扱う。公開リポジトリの最新コミットに対する自動検証は、[GitHub ActionsのValidate](https://github.com/Eonshore/humanizer-ja-editor/actions/workflows/validate.yml)で確認できる。GitHub上で取得できることは、文章品質の追加の証明にはならない。
